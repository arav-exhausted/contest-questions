#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        vector<int> members(n);
        vector<int> store(n);

        for(auto i=members.begin(); i != members.end();i++){
            
        }




















    }

}
